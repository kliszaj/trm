# Copy to .env for production builds
# ESP32 IP address (or trm.local if mDNS works on your network)
VITE_ESP32_URL=http://192.168.1.XX

# Set to true to use the in-memory mock instead of the real ESP32 API
# VITE_USE_MOCK=false
